#Copyright (C) 2007 L. Donnie Smith

include @top_builddir@/defs.mak

APP_NAME = wmgui

SOURCES = main.c interface.c support.c

CFLAGS += @GTK_CFLAGS@ -I@top_builddir@/wiimote \
	-DCWIID_VERSION=\"${PACKAGE_VERSION}\"
LDFLAGS += -L@top_builddir@/wiimote
LDLIBS += @GTK_LIBS@ -lm -lwiimote
INST_DIR = @bindir@

include $(COMMON)/include/app.mak

distclean: clean
	rm Makefile

.PHONY: distclean
